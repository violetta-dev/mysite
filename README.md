
# VIX Systems — v4.5

Deploy rapido:
1. Carica **tutti i file** nella root della repo (mantieni `/assets` com'è).
2. Su Netlify: Deploy → (se serve) *Clear cache and redeploy*.
3. Su GitHub Pages: attiva Pages (root).

Note:
- CSS è `style-v45.css` (bypassa cache legacy).
- Video in `/assets/bg.mp4`.
- Footer: © VIX 2025 — Neural Core.
